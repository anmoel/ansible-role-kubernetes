---

- hosts: kubernetes
  become: true
  become_user: root
  roles:
    - geerlingguy.docker
    - { role: evrardjp.keepalived, when: kubernetes_master }
    - { role: ansible-role-kubernetes, tags: k8s }
